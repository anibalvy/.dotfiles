ip addr # Display all interfaces with addresses

ip -s a  # Display Statistics on all interface

ip link set dev wlan0 {up|down}       # Take down / up the wireless adapter

ip addr add 192.168.1.100/32 dev eth0 # Set a static IP and netmask

ip addr del 192.168.1.100/32 dev eth0 # Remove a IP from an interface

ip address flush dev eth0             # Remove all IPs from an interface

ip route      # Display all routes

ip -6 route   # Display all routes for IPv6

ip route add default via 192.168.1.1   # Add default route via gateway IP

ip route add 192.168.0.0/24 dev eth0   # Add route via interface

ip route get 192.168.0.1            # Get route 

ip link set dev eth0 address aa:bb:cc:dd:ee:ff   # Change your mac address

ip neighbor show      # View neighbors (using ARP and NDP)

ip a s eth0 	#Find the current ip address for the eth0 interface in Linux

# edit /etc/iproute2/rt_tables
# to add roting table alias like:
# 102 rt_interal

ip route show table rt_internal # print routing table 102 called rt_internal

# DHCP client to release and renew an IP address as per your dhcp client name
# Method 1
dhclient -v -r eth0 	Force Linux to renew IP address using a DHCP for eth0 interface
# Method 2
systemctl restart network.service 	Restart networking service and obtain a new IP address via DHCP on Ubuntu/Debian Linux
systemctl restart networking.service 	Restart networking service and obtain a new IP address via DHCP on a CentOS/RHEL/Fedora Linux
# Method 3
nmcli con 	Use NetworkManager to obtain info about Linux IP address and interfaces
nmcli con down id 'enp6s0' 	Take down Linux interface enp6s0 and release IP address in Linux
nmcli con up id 'enp6s0' 	Obtain a new IP address for Linux interface enp6s0 and release IP address using DHCP
# Method 4
sudo dhcpcd -k 	Use the dhcpcd as the client to obtain a new release
sudo dhcpcd 	Get the new one.
sudo dhcpcd -k eth0 && dhcpcd -n eth0 	Get a new IP address for Linux interface called eth0 and release IP address using the dhcpcd as the client.

ip -br -c a  # how ips brief

