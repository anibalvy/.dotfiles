# Force GPT 

At install time, at boot options in text mode, it is needed to add the option "inst.gpt" as:

```sh
> vmlinuz initrd=initrd.img inst.stage2=hd:LABEL=CentOS\x207\x20x86_64 rd.live quiet inst.gpt

````
