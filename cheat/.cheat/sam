# AWS SAM - Serveless Application Model 
# create lambdas

sam init            # start project

# create python app and in requirements add modules

sam build           # will create .aws-sam with runtime config, then it cam be test locally and deploy
sam local start-api # test locally

# Commands you can use next
sam validate                                  # Validate SAM template: 
sam sync --stack-name {{stack-name}} --watch  # [*] Test Function in the Cloud: 

sam deploy --guided
