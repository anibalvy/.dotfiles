
// Hoisting: var (variables) get hoisted to the top of the scope by the js engine.
// so next code will not give an error like with let. But only declaration is hoisted and not the value assign to it.
console.log(a)
var a = 1

// arrow functions 1 option
const add = (a, b)=> {
    return a + b
}
