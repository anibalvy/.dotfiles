- To create a symbolic link:
ln -s path/original_element path/name-of-the-symlink

- Symlink, while overwriting existing destination files
ln -sf path/original_element path/name-of-the-symlink

> To create a hard link drop the "-s" option 

> hard link must be on the same physical partition
